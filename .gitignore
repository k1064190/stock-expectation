# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Environment
.env
.venv/
venv/

# Data
data/*.db
data/*.db-wal
data/*.db-shm
# Watchlist monitor DB (covered by data/*.db, listed for clarity)
data/watchlist.db

# Watchlist monitor dedup/cooldown state (runtime, not tracked)
state/
state/watchlist_alerts.json

# Sector-rotation RS snapshots (regenerated per run by `stock-cli sector-rs --write`)
data/sector_rs_us.json
data/sector_rs_kr.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Reference repo (large, clone separately)
claude-trading-skills/

# Cloned external skill repos (analyzed in docs/external-skills-analysis.md, not tracked)
references/repos/

# Stage 7-A mem0/Qdrant data
data/memory/

# Stage 7-B Neo4j data + plugins
data/neo4j/

# Macro-news (GDELT) response cache — runtime artifact, regenerable
data/cache/

# Local env (NEO4J_PASSWORD etc.)
.env

# External repos cloned for benchmarking/analysis (not part of this project)
research/external/
